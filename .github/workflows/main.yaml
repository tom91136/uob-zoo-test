name: CI
on: [push]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      # - name: Free Disk Space (Ubuntu)
      #   uses: jlumbroso/free-disk-space@main
      #   with:
      #     tool-cache: false
      #     android: true
      #     dotnet: true
      #     haskell: true
      #     large-packages: true
      #     docker-images: true
      #     swap-storage: true

      - name: Before
        run: |
          set -xe
          df -H

      - name: Removing stuff
        run: |
          set -xe
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/share/swift
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/.ghcup
          sudo rm -rf "/usr/local/share/boost"
          sudo rm -rf "$AGENT_TOOLSDIRECTORY"
          sudo rm -rf /opt/hostedtoolcache/
          sudo rm -rf /usr/local/graalvm/
          sudo rm -rf /usr/local/share/powershell
          sudo rm -rf /usr/local/share/chromium
          sudo rm -rf /usr/local/lib/node_modules
          sudo docker image prune --all --force

          APT_PARAMS='sudo apt -y -qq -o=Dpkg::Use-Pty=0'
          $APT_PARAMS remove -y '^dotnet-.*'
          $APT_PARAMS remove -y '^llvm-.*'
          $APT_PARAMS remove -y '^php.*'
          $APT_PARAMS remove -y '^mongodb-.*'
          $APT_PARAMS remove -y '^mysql-.*'
          $APT_PARAMS remove -y azure-cli google-* google-chrome-stable firefox mono-devel libgl1-mesa-dri
          $APT_PARAMS autoremove --purge -y
          $APT_PARAMS autoclean
          $APT_PARAMS clean

      - name: After
        run: |
          set -xe
          df -H

      - name: Enable zram
        run: |
          sudo swapoff -a
          sudo rm -f /mnt/swapfile
          sudo apt-get install -y util-linux zram-config linux-modules-extra-$(uname -r)

          sudo modprobe zram

          totalmem=`LC_ALL=C free | grep -e "^Mem:" | sed -e 's/^Mem: *//' -e 's/  *.*//'`
          mem=$((totalmem * 4 * 1024))

          sudo echo $mem > /sys/block/zram0/disksize
          sudo mkswap /dev/zram0
          sudo swapon -p 5 /dev/zram0
          sudo zramctl

      - name: Checkout
        uses: actions/checkout@v4
        if: always()

      - name: Enable KVM group perms # See https://github.com/actions/runner-images/issues/7670
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
          sudo apt-get update
          sudo apt-get install -y --no-install-suggests libvirt-clients libvirt-daemon-system libvirt-daemon virtinst bridge-utils qemu-system-x86 qemu-system-aarch64
          sudo usermod -a -G kvm,libvirt $USER
        if: always()

      - name: Generate keys
        run: ssh-keygen -t ed25519 -N "" -f $HOME/.ssh/id_ed25519
        if: always()
      ################

      - name: Tailscale
        uses: tailscale/github-action@v2
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:example
        if: always()

      - name: Install tools
        run: |
          sudo apt-get install -y tmux micro dstat nload
          cd
          git clone https://github.com/gpakosz/.tmux.git
          ln -s -f .tmux/.tmux.conf
          cp .tmux/.tmux.conf.local .
        if: always()

      - name: Wait for SSH
        run: |
          sudo sed -i 's/PasswordAuthentication yes/PasswordAuthentication no/g' /etc/ssh/sshd_config
          sudo systemctl restart ssh
           
          mkdir -p $HOME/.ssh
          touch $HOME/.ssh/authorized_keys 
          printf "%s\n" "${{ secrets.PUBLIC_KEY }}" >> $HOME/.ssh/authorized_keys
          sudo chown runner:runner $HOME/.ssh/authorized_keys
          chmod go-w ~/
          chmod 700 ~/.ssh
          chmod 600 ~/.ssh/authorized_keys
        if: always()
      ################

      # - name: Install dependencies
      #   run: sudo apt-get install -y cloud-utils libvirt-dev

      # - name: Install packer and vagrant
      #   run: |
      #     wget -O- https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
      #     echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
      #     sudo apt-get update -y
      #     sudo apt-get install -y packer vagrant

      # - name: Setup vagrant plugin and binfmt
      #   run: |
      #     vagrant plugin install vagrant-libvirt
      #     sudo apt-get reinstall -y qemu-user-static
      #     sudo pip3 install passlib --break-system-packages # python3-passlib didn't work

      # - name: Stats
      #   run: df -H

      # - name: Create proxmox/router image
      #   run: |
      #     (
      #       cd images
      #       time make opnsense.qcow2
      #     ) &
      #     (
      #       cd proxmox
      #       time make build-libvirt
      #       vagrant box add -f proxmox-ve-amd64 proxmox-ve-amd64-libvirt.box.json
      #       make clean
      #       rm -rf *.box
      #     ) & wait
      #   if: always()

      # - name: Build opnsense image
      #   working-directory: images
      #   run: |
      #     make opnsense.qcow2
      #   if: always()

      # - name: Build bos image
      #   working-directory: images
      #   run: time make bos.x86_64.qcow2  cos.x86_64.warewulf.tar -j
      #   if: always()

      # - name: Build cos image
      #   working-directory: images
      #   run: time make bos.aarch64.qcow2 cos.aarch64.warewulf.tar -j
      #   if: always()

      # - name: Clean up image
      #   working-directory: images
      #   run: |
      #     # make all -j
      #     # make cos.x86_64.warewulf.tar cos.aarch64.warewulf.tar bos.x86_64.qcow2 bos.aarch64.qcow2 opnsense.qcow2
      #     time rm -rf *.iso
      #   if: always()

      # - name: Stats
      #   run: df -H
      #   if: always()

      - name: Stats
        run: df -H

      # - name: Run provision
      #   run: vagrant up

      - name: Stats
        run: df -H

      - name: Wait for SSH
        run: sleep infinity
        if: always()
